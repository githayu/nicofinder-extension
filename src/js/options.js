import ReactDOM from 'react-dom';
import { Options } from 'src/js/components/';

const container = document.createElement('div');

ReactDOM.render(<Options />, container);

document.body.appendChild(container);